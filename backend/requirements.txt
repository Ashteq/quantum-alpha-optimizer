# ─────────────────────────────────────────────────────────────────
# Quantum-Alpha Optimizer — Backend Dependencies
# Python 3.10+ recommended
#
# Install:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# ── API framework ────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# ── Market data ──────────────────────────────────────────────────
yfinance==0.2.40
pandas==2.2.2
numpy==1.26.4

# ── Quantum computing (Qiskit v1.x ecosystem) ────────────────────
# Core Qiskit runtime
qiskit==1.1.0
qiskit-aer==0.14.2               # local statevector / shot simulators

# Algorithms (includes QAOA, VQE, COBYLA wrapper)
qiskit-algorithms==0.3.0

# Finance primitives (PortfolioOptimization)
qiskit-finance==0.4.0

# Optimisation (QUBO / MinimumEigenOptimizer)
qiskit-optimization==0.6.1

# ── Utilities ────────────────────────────────────────────────────
httpx==0.27.0                    # optional — for integration tests
python-multipart==0.0.9          # required by FastAPI form-data support
